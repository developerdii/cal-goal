# Supabase project credentials (Dashboard → Project Settings → API).
# These are PUBLIC (anon) values — safe to expose in the client bundle.
# The service_role key must NEVER be placed here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
